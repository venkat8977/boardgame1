apiVersion: v1
kind: Service
metadata:
  name: boardgame-svc
spec:
  selector:
    app: boardgame
  ports:
    -name: http
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}